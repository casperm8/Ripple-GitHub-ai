import { useState, useEffect } from 'react'\nimport { useQuery } from 'react-query'\nimport axios from 'axios'\n\nconst BrowseCodes = () => {\n  const [filters, setFilters] = useState({\n    platform: '',\n    search: '',\n    page: 1\n  })\n\n  const { data: codesData, isLoading } = useQuery(\n    ['codes', filters],\n    async () => {\n      const params = new URLSearchParams()\n      if (filters.platform) params.append('platform', filters.platform)\n      if (filters.search) params.append('search', filters.search)\n      params.append('page', filters.page)\n      params.append('limit', 12)\n\n      const response = await axios.get(`/api/referral-codes?${params}`)\n      return response.data\n    },\n    { keepPreviousData: true }\n  )\n\n  const handleCopyCode = (code) => {\n    navigator.clipboard.writeText(code)\n    alert('Code copied to clipboard!')\n  }\n\n  const platforms = ['uber', 'airbnb', 'doordash', 'lyft', 'shopify', 'amazon', 'other']\n\n  return (\n    <div className=\"min-h-screen bg-gray-50 py-12\">\n      <div className=\"container mx-auto px-4\">\n        <h1 className=\"text-4xl font-bold mb-8\">Browse Referral Codes</h1>\n\n        <div className=\"grid md:grid-cols-4 gap-6 mb-8\">\n          {/* Search */}\n          <div className=\"md:col-span-2\">\n            <input\n              type=\"text\"\n              placeholder=\"Search codes...\"\n              value={filters.search}\n              onChange={(e) => setFilters({ ...filters, search: e.target.value, page: 1 })}\n              className=\"w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600\"\n            />\n          </div>\n\n          {/* Platform Filter */}\n          <select\n            value={filters.platform}\n            onChange={(e) => setFilters({ ...filters, platform: e.target.value, page: 1 })}\n            className=\"px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600\"\n          >\n            <option value=\"\">All Platforms</option>\n            {platforms.map(p => (\n              <option key={p} value={p}>{p.charAt(0).toUpperCase() + p.slice(1)}</option>\n            ))}\n          </select>\n        </div>\n\n        {/* Codes Grid */}\n        {isLoading ? (\n          <div className=\"text-center py-12\">\n            <p className=\"text-xl text-gray-600\">Loading codes...</p>\n          </div>\n        ) : (\n          <>\n            <div className=\"grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8\">\n              {codesData?.codes?.map(code => (\n                <div key={code._id} className=\"bg-white p-6 rounded-lg shadow-md hover:shadow-lg transition\">\n                  <div className=\"flex justify-between items-start mb-4\">\n                    <div>\n                      <h3 className=\"text-lg font-bold text-blue-600\">{code.platform.toUpperCase()}</h3>\n                      <p className=\"text-sm text-gray-500\">by @{code.owner?.username}</p>\n                    </div>\n                    <div className=\"text-right\">\n                      <p className=\"text-xs text-gray-500\">👁️ {code.views}</p>\n                      <p className=\"text-xs text-gray-500\">📤 {code.shares}</p>\n                    </div>\n                  </div>\n\n                  <p className=\"text-gray-600 mb-2 text-sm\">{code.description}</p>\n                  <p className=\"text-lg font-bold text-green-600 mb-4\">{code.discount}</p>\n\n                  <code className=\"bg-gray-100 p-2 rounded block text-center font-mono text-sm mb-4 cursor-pointer hover:bg-gray-200\">\n                    {code.code}\n                  </code>\n\n                  {code.expiryDate && (\n                    <p className=\"text-xs text-gray-500 mb-4\">\n                      Expires: {new Date(code.expiryDate).toLocaleDateString()}\n                    </p>\n                  )}\n\n                  <div className=\"flex gap-2\">\n                    <button\n                      onClick={() => handleCopyCode(code.code)}\n                      className=\"flex-1 bg-blue-600 text-white py-2 rounded hover:bg-blue-700 text-sm font-medium\"\n                    >\n                      Copy Code\n                    </button>\n                    <button className=\"flex-1 bg-gray-200 text-gray-800 py-2 rounded hover:bg-gray-300 text-sm font-medium\">\n                      📤 Share\n                    </button>\n                  </div>\n                </div>\n              ))}\n            </div>\n\n            {/* Pagination */}\n            {codesData?.pages > 1 && (\n              <div className=\"flex justify-center gap-4\">\n                <button\n                  disabled={filters.page === 1}\n                  onClick={() => setFilters({ ...filters, page: filters.page - 1 })}\n                  className=\"px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 disabled:opacity-50\"\n                >\n                  Previous\n                </button>\n                <span className=\"px-4 py-2 text-gray-600\">\n                  Page {filters.page} of {codesData?.pages}\n                </span>\n                <button\n                  disabled={filters.page === codesData?.pages}\n                  onClick={() => setFilters({ ...filters, page: filters.page + 1 })}\n                  className=\"px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 disabled:opacity-50\"\n                >\n                  Next\n                </button>\n              </div>\n            )}\n          </>\n        )}\n      </div>\n    </div>\n  )\n}\n\nexport default BrowseCodes\n